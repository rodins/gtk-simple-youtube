// ColumnsEnum.hpp

// For tree views
enum {
	IMAGE_COLUMN,
	TITLE_COLUMN,
	NUM_COLS
};

// For iconView and results model
enum {
	ICON_IMAGE_COLUMN,
	ICON_TITLE_COLUMN,
	ICON_VIDEO_ID,
	ICON_IMAGE_LINK,
	ICON_NUM_COLS
};
